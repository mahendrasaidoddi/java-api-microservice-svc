name: Java CI

on:
    push:
        branches: [ test ]
    pull_request:
        branches: [ test ]

jobs:
    hello-world:
        runs-on: ubuntu-latest
        steps:
            - name: Print Hello World
              run: echo "Hello World"